let email = "";
document.querySelector("#requestForm").addEventListener("submit", async event => { event.preventDefault(); email = document.querySelector("#emailInput").value; const response = await fetch("/api/auth/request-password-reset", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email }) }); if (!response.ok) { document.querySelector("#status").textContent = "No se pudo solicitar el código. Intenta de nuevo."; return; } document.querySelector("#requestForm").hidden = true; document.querySelector("#resetForm").hidden = false; document.querySelector("#helpText").textContent = "Revisa tu correo e introduce el código de seis dígitos."; document.querySelector("#status").textContent = "Si la cuenta existe, el código fue enviado."; });
document.querySelector("#resetForm").addEventListener("submit", async event => { event.preventDefault(); const response = await fetch("/api/auth/reset-password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email, code: document.querySelector("#codeInput").value, password: document.querySelector("#passwordInput").value }) }); if (!response.ok) { document.querySelector("#status").textContent = (await response.json()).error; return; } window.location.href = "account.html"; });
