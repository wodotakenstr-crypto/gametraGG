document.querySelector("#smtpForm").addEventListener("submit", async event => { event.preventDefault(); const status = document.querySelector("#status"); status.textContent = "Probando conexión..."; const response = await fetch("/api/setup/smtp", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(new FormData(event.currentTarget))) }); const result = await response.json(); status.textContent = response.ok ? "Brevo conectado. Ya puedes volver y reenviar el código." : result.error; status.style.color = response.ok ? "#426d28" : "#a44330"; });
