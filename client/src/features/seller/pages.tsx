import { useEffect, useState, type FormEvent } from "react";
import { api, json } from "../../lib/api";
import type { Offer } from "../../lib/types";

export function SellerOffersPage() { const [offers, setOffers] = useState<Offer[]>([]); const [status, setStatus] = useState(""); async function load() { try { setOffers(await api<Offer[]>("/seller/offers")); } catch (error) { setStatus(error instanceof Error ? error.message : "No se pudieron cargar las ofertas."); } } useEffect(() => { void load(); }, []); async function pause(id: string) { try { await api(`/offers/${id}/pause`, { method: "POST" }); await load(); } catch (error) { setStatus(error instanceof Error ? error.message : "No se pudo actualizar la oferta."); } } return <section className="mx-auto max-w-5xl px-5 py-12"><p className="eyebrow">PANEL DE VENDEDOR</p><h1 className="mt-2 text-4xl font-bold">Mis ofertas</h1><p className="mt-2 text-slate-400">Gestiona tus publicaciones activas y pausadas.</p>{status && <p className="notice-error mt-4">{status}</p>}<div className="mt-8 grid gap-4">{offers.map(offer => <article className="card flex flex-wrap items-center justify-between gap-4" key={offer.id}><div><p className="text-sm text-lime-300">{offer.game} · {offer.type}</p><h2 className="font-semibold">{offer.title}</h2><p className="text-sm text-slate-400">${Number(offer.price).toFixed(2)} · {offer.delivery}</p></div><button className="button-secondary" onClick={() => void pause(offer.id)}>{offer.paused ? "Reanudar" : "Pausar"}</button></article>)}{!offers.length && <div className="card text-slate-400">Aún no has publicado ofertas.</div>}</div></section>; }

interface Wallet { availableUsdt: number; pendingWithdrawalUsdt: number; withdrawalAddress?: string; credits?: unknown[]; withdrawals?: unknown[]; }
export function WalletPage() { const [wallet, setWallet] = useState<Wallet | null>(null); const [status, setStatus] = useState(""); async function load() { try { setWallet(await api<Wallet>("/seller/wallet")); } catch (error) { setStatus(error instanceof Error ? error.message : "No se pudo cargar la billetera."); } } useEffect(() => { void load(); }, []); async function address(event: FormEvent<HTMLFormElement>) { event.preventDefault(); try { await api("/seller/wallet/address", { method: "PUT", ...json(Object.fromEntries(new FormData(event.currentTarget))) }); setStatus("Dirección guardada."); await load(); } catch (error) { setStatus(error instanceof Error ? error.message : "No fue posible guardar la dirección."); } } async function withdrawal(event: FormEvent<HTMLFormElement>) { event.preventDefault(); try { await api("/seller/wallet/withdrawals", { method: "POST", ...json(Object.fromEntries(new FormData(event.currentTarget))) }); setStatus("Retiro solicitado."); await load(); } catch (error) { setStatus(error instanceof Error ? error.message : "No fue posible solicitar el retiro."); } } return <section className="mx-auto max-w-5xl px-5 py-12"><p className="eyebrow">BILLETERA DE VENDEDOR</p><h1 className="mt-2 text-4xl font-bold">Saldo USDT</h1><div className="mt-8 grid gap-4 md:grid-cols-2"><div className="card"><p className="text-slate-400">Disponible</p><strong className="mt-2 block text-4xl">{Number(wallet?.availableUsdt ?? 0).toFixed(2)} USDT</strong></div><div className="card"><p className="text-slate-400">Retenido en retiros</p><strong className="mt-2 block text-4xl">{Number(wallet?.pendingWithdrawalUsdt ?? 0).toFixed(2)} USDT</strong></div></div><div className="mt-5 grid gap-5 md:grid-cols-2"><form className="card space-y-4" onSubmit={address}><h2 className="text-xl font-semibold">Dirección de retiro</h2><input className="field" name="address" defaultValue={wallet?.withdrawalAddress} required maxLength={34} placeholder="Dirección TRON TRC20" /><button className="button-primary">Guardar dirección</button></form><form className="card space-y-4" onSubmit={withdrawal}><h2 className="text-xl font-semibold">Solicitar retiro</h2><input className="field" name="amount" type="number" min="10" step="0.01" required placeholder="Importe USDT" /><button className="button-primary">Solicitar retiro</button></form></div>{status && <p className="mt-4 text-sm text-slate-300">{status}</p>}</section>; }
